# Components to Launch

Make sure that you activate a dev server that is reachable beyond your laptop. This is done using cloudflare without a login credential.

```
npm run dev -- --host --port 5173
```

This will create a host from your laptop and make it so that other devices can connect to the program/software.