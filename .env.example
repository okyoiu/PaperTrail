# Frontend (safe to expose — protected by Supabase RLS policies)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-only (used in /api/places.js, NEVER prefix with VITE_ or it leaks to the browser)
GOOGLE_PLACES_API_KEY=
