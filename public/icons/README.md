Drop `icon-192.png` and `icon-512.png` (app icon, any square artwork) here, then list
them in the `manifest.icons` array in `vite.config.js` so the "Add to Home Screen"
install prompt gets a real icon instead of the browser default.
